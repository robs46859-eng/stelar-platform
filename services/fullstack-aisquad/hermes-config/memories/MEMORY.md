fscompanion project moved to /home/azureuser/family-companion/backend/ (monorepo). ~55 source files: core infra, 16 models, schemas, services, agent swarm (10), Gemma/LLM dual-mode (local Gemma 4 26B Ollama + Gemini fallback + ModelRouter), 13 API routes (12 original + android). LLM now wired into support_agent service. Git remote: robs46859-eng/fscompanion.
§
User preferences: skip Instagram monitoring for the current phase; use Hermes profiles and local bin wrappers; Chromium via Snap with sandbox-disabling flags; limit delegated tasks to ≤3 concurrent children; rely on web search for source discovery; store outputs in /home/azureuser/hermes-workspace; do not persist or expose any credentials; avoid mock/placeholders in deliverables; value factual accuracy, brand enforcement in deliverables; want product manuals to include specifications and troubleshooting.
§
family-companion monorepo: /home/azureuser/family-companion/{backend,android}. Backend has dual-mode GemmaClient (local Gemma 4 26B Ollama + cloud Gemini fallback) with ModelRouter, 3 new models (Resource with maternal categories, PregnancyJourney, HealthMetric with contraction/kick_count types), 12 Android API endpoints at /api/v1/android, and LLM now wired into support_agent service. android/ cloned from github.com/robs46859-eng/navigate.git. Git remotes: backend → robs46859-eng/fscompanion, android → robs46859-eng/navigate. Ollama model tag is gemma4:26b (NOT gemma4:27b which doesn't exist).
§
Azure VM gemmaco-key: 8GB RAM, no GPU, Ubuntu. Ollama 0.24.0 installed. Gemma 4 26B model (~17GB) downloaded and stored on 1TB managed disk at /mnt/gemma4/ollama. Ollama service configured via systemd with OLLAMA_MODELS=/mnt/gemma4/ollama. Model cannot run on current VM (needs 18.3GB RAM, only 5.3GB available). Next step: resize to NC8as_T4_v3 or similar GPU VM, reattach managed disk, no re-pull needed.